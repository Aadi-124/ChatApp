#spring.application.name=ChatApp
#
#spring.datasource.url=jdbc:mysql://localhost:3306/ChatApp
#spring.datasource.username=root
#spring.datasource.password=AadityaT@8275
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
#
#
##"Don't modify my table and column names. Just use them exactly as defined in my Java entity classes."
#spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl


# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/ChatApp
spring.datasource.username=postgres
spring.datasource.password=admin
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Hibernate (Optional, if you use JPA/Hibernate)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Connection Pool (Optional, if you want to configure connection pool settings)
#spring.datasource.hikari.maximum-pool-size=10  # Set the pool size
#spring.datasource.hikari.idle-timeout=600000   # Time before an idle connection is closed

# Optional: Enable SSL if needed (this depends on your PostgreSQL setup)
# spring.datasource.url=jdbc:postgresql://localhost:5432/your_database_name?sslmode=require
